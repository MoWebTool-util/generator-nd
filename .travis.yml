language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-nd' ]; then cd .. && eval "mv $currentfolder generator-nd" && cd generator-nd; fi
